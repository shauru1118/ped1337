services:
  web:
    build: .
    image: ped1337:latest
    ports:
      - "${PORT:-8000}:8000"
    environment:
      PORT: "8000"
      HOST: "0.0.0.0"
      WRITABLE_DIR: /tmp/ped1337
      TEMP_DIR_PATH: /tmp/ped1337/temp
      KEY_FILE_PATH: /tmp/ped1337/key.key
      GENERATED_STATIC_DIR: /tmp/ped1337/generated
      RUN_TELEGRAM_BOT: ${RUN_TELEGRAM_BOT:-false}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      ADMIN_CHAT_ID: ${ADMIN_CHAT_ID:-}
      DEFAULT_KEY_B64: ${DEFAULT_KEY_B64:-}
      CLEANUP_MAX_AGE_SECONDS: ${CLEANUP_MAX_AGE_SECONDS:-300}
      CLEANUP_INTERVAL_SECONDS: ${CLEANUP_INTERVAL_SECONDS:-60}
    volumes:
      - app-writable:/tmp/ped1337
    restart: unless-stopped
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=3)",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

  bot:
    profiles: ["bot"]
    build: .
    image: ped1337:latest
    command: ["python", "-m", "bot"]
    environment:
      WRITABLE_DIR: /tmp/ped1337
      TEMP_DIR_PATH: /tmp/ped1337/temp
      KEY_FILE_PATH: /tmp/ped1337/key.key
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      ADMIN_CHAT_ID: ${ADMIN_CHAT_ID:-}
      DEFAULT_KEY_B64: ${DEFAULT_KEY_B64:-}
    volumes:
      - app-writable:/tmp/ped1337
    restart: unless-stopped

volumes:
  app-writable:
